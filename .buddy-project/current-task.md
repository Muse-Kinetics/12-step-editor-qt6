# Current Task

Migration of 12 Step to the WMS/WinMM dual-backend + chunked firmware-update architecture is **code-complete and clean-build-validated** (2026-07-30), following `../SOP-WMS-and-Chunked-Firmware-Update-Migration.md`. Hardware validation (SOP §5) has not been done — that's the only remaining step.

## IMPORTANT — branch/remote migration (2026-07-31)

Discovered the working repo's `origin` (Bitbucket, `keith-mcmillen-instruments/12-step-editor-qt6`) was **never the authoritative repo** — the user cleaned up and open-sourced this project to `github.com/Muse-Kinetics/12-step-editor-qt6` back in December 2025 from a separate working folder, but this Dropbox-synced repo's remote was never repointed. All prior work in this document (and everything above this note) happened on `main_win`, tracking Bitbucket, which has **zero shared git history** with `github/main` (confirmed via `git merge-base` — no common ancestor). Bitbucket access has since lapsed.

Content-level diff between `main_win` and `github/main` was small and explainable (MPL license headers, OSS README rewrite, binary cleanup, independently-done submodule consolidation on the GitHub side — see decisions.md). Everything from this session was re-applied by hand onto a new local branch `main` (tracking `github/main`), file by file, rather than blind-patched, since both sides had independently touched some of the same files (e.g. submodule consolidation happened twice, differently). `main_win` and its Bitbucket tracking are left untouched as a safety net; the session's original uncommitted work is preserved in `git stash@{0}` on that branch. **`github/main` is now the authoritative base going forward.** Nothing has been committed or pushed — all porting was done as uncommitted working-tree changes for the user's review, per explicit instruction.

## What's done

1. **`.gitmodules` cleanup**: removed 4 stale `qt-qunexus/inc/*` entries (no matching working-tree dirs). Fixed URL/commit mismatches for both `rtmidi` and `KMI_KMDM` — both were declared against stale/wrong-history mirrors (`thestk/rtmidi.git`, `bitbucket.org/.../kmi_midi_device_manager.git`) while actually needing the active `github.com:Muse-Kinetics/*` forks. This mismatch pattern applied to **both** submodules, not just rtmidi as the SOP's §6.1 called out — see blockers.md.
2. **Branch-drift analysis (§3)**: diffed `KMI_MDM`'s `12Step` branch against `main` at the SoftStep reference repo. Found the drift was almost entirely license-header/README noise plus dead/commented-out code already inactive on both sides. `main` is already a generic multi-product device manager with full 12 Step awareness (`PID_12STEP1/2/_BL`, product-name handling). **No product-specific fixes needed porting forward** — pointed `KMI_KMDM` directly at `main` tip `c5c549b`.
3. **Submodule consolidation (§4.1)**: verified `KMI_Ports`/`KMI_Updates`/`MidiDeviceManager` public API is unchanged in `main` (all symbols `mainwindow.cpp` calls still exist) before removing anything. Retired `QT/inc/KMI_Ports` and `QT/inc/KMI_Updates` submodules. Relocated the SSL DLLs they carried (`libcrypto-1_1-x64.dll`, `libssl-1_1-x64.dll`, used for the software-update HTTPS check) to `QT/ssl/` since `main`'s `KMI_KMDM` doesn't carry them. Rewrote `.pro` INCLUDEPATH/SOURCES/HEADERS/DISTFILES to the single consolidated `KMI_KMDM` tree.
4. **rtmidi → WMS fork (§4.2)**: `QT/inc/rtmidi` now on fork's `WMS` branch, commit `b79b83e`.
5. **Build system (§4.3)**: `QT/12StepEditor.pro` `win32{}` block now has the full WMS SDK/WinRT-projection wiring (copied from SoftStep's `SoftStepAdvanced.pro`), `__WINDOWS_MM__=1` + `__WINDOWS_MIDI_SERVICES__=1`, and the new linker flags. No UWP backend existed to remove (12 Step's old UWP block was already dead/commented).
6. **Chunked firmware (§4.5)**: added `mk_firmware_tester` (+ its nested `sendsysex`/`midi_cpp` submodules) at repo root per user instruction. Copied `sendsysex/syx/12Step/12 Step Firmware v1.0.8_cs512.syx` into `QT/resources/firmware/12Step_Firmware_v1.0.8-cs512.syx`, updated `resources.qrc`, bumped `_fw_ver_12step` to `1,0,8` in `KMI_FwVersions.h` (inside the `KMI_KMDM` submodule — **not yet committed there**, see blockers.md), updated the firmware filename pattern in `mainwindow.cpp`, and removed the old ad hoc `sysExTxChunkSize=48`/`sysExTxChunkDelay=1` override (the packetized system uses its own dedicated `sysExTxChunkSizeFW` internally now).
7. **Pre-1.0.0 guard (§4.6)**: ported into `slotFirmwareDetected` in `mainwindow.cpp`, using 12 Step's existing `UserDialog` convention rather than SoftStep's `slotCreateDialog` (which 12 Step doesn't have).
8. **Diagnostics (§4.7)**: added `DiagnosticLogger::initialize()` call in `mainwindow.cpp`'s constructor (right after the org/app name calls, since unlike SoftStep, 12 Step sets those in `mainwindow.cpp` rather than `main.cpp`). Confirmed this installs a Qt message handler that captures *all* `qDebug`/`qWarning` output automatically — no need to rewrite individual log calls. Also ported the Help-menu **"Open Log File Location"** action (`slotOpenLogDirectory()`, opens `DiagnosticLogger::logDirectoryPath()` via `QDesktopServices`) from SoftStep's `mainwindow.cpp` — this wasn't in the SOP's §4.7 text explicitly but is part of the same diagnosticlogger adoption in SoftStep and was flagged separately by the user. Rebuilt after adding it — compiled and linked clean (incremental build).
9. **VS Code tooling (§4.8)**: new `.vscode/` with `run-msvc-qmake-task.cmd`, `launch-and-tail.ps1`, `tail-log.ps1` (copied unchanged — Qt 6.3.2/MSVC2019 matches SoftStep's), `tasks.json`/`launch.json` retargeted for `QT/12StepEditor.pro` → `12 Step Editor.exe`, app-data org name `KeithMcMillenInstruments` (no spaces, confirmed from the actual `setOrganizationName` call).
10. **Clean build validated**: ran a real `qmake` + `nmake` build on this machine (Qt 6.3.2, MSVC2019, WMS SDK Runtime + Windows SDK 10.0.26100.0 all present) — **compiled and linked successfully**, `12 Step Editor.exe` produced, WinRT projection headers generated correctly, SSL DLLs copied from the new `QT/ssl/` path.

## Important fix after first hardware smoke-test (2026-07-31)

The first real console log (WMS backend, real hardware) showed the firmware file being sent as **one 88KB blob via the legacy `slotSendSysExBA` path**, chunked at 48 bytes with **no per-packet identity request/reply** — i.e. the packetized transport wasn't actually active. Root cause: `MidiDeviceManager::isPacketizedFirmwareUpdateEnabled()` in `KMI_mdm.cpp` only auto-enables the packetized handshake system for SoftStep PIDs by default; every other product must opt in explicitly via `slotSetFirmwareUpdateTransportMode(FW_TRANSPORT_PACKETIZED)`. Missed this during the original §4.5 pass — the file/`.qrc`/version-bump work was correct, but the actual transport switch was never flipped. Fixed by calling `TwelveStep->slotSetFirmwareUpdateTransportMode(FW_TRANSPORT_PACKETIZED);` right after `TwelveStep` is constructed in `mainwindow.cpp`. Rebuilt clean.

Also clarified for the record: the `48` byte chunk size seen in the log (`sysExTxChunkSizeFW`) is a *different, lower-level* setting — the MIDI-transport wire-slicing size used only by the legacy blob-send path, tied to the 64-byte USB MIDI packet limit. It has nothing to do with the `-cs512` firmware packet size. The packetized path's `sendSysExImmediate()` sends each full pre-chunked packet (up to 512 bytes) in a single `midi_out->sendMessage()` call with no further slicing — so enabling packetized mode was the complete fix; `sysExTxChunkSizeFW` did not need changing.

## Installer/deploy tooling added (2026-07-31, not part of the SOP, requested separately)

12 Step's `.pro` file already had its own self-contained `deploy` qmake target (unlike SoftStep, which uses a standalone PowerShell script) — but it signs unconditionally and there was no VS Code task for it at all. Added:
- `deploy_nosign` extra target in `QT/12StepEditor.pro` (mirrors `deploy` but skips all `signtool` calls) — dormant unless `DEPLOY` is uncommented (see below).
- `.vscode/make-installer.ps1`, a standalone script mirroring SoftStep's `.vscode/make-installer.ps1` structure/functions but adapted to 12 Step's single-editor/single-package layout (`win-build/packages/com.keithmcmillen.12stepeditor`), using the same signing cert thumbprint and tool paths already baked into `12StepEditor.pro`. Takes `-DisableSigning`. Syncs `config.xml`/`package.xml` version from the `.pro` `VERSION` (12 Step's old embedded deploy target didn't do this).
- VS Code tasks: "Make 12 Step Windows Installer" / "(No Signing)", "Deploy 12 Step Windows Installer" / "(No Signing)" (the latter two chain Build Release → Make Installer), mirroring SoftStep's four-task pattern.
- **`QT/12StepEditor.pro` has `DEPLOY` commented out** (`#DEPLOY = 1`) — found already in that state mid-session (not something I changed; confirmed via `git diff`). User confirmed intentional: going forward they'll primarily use VS Code + `make-installer.ps1` for packaging, and Qt Creator / the `.pro` file mainly for adjusting UI in the visual designer. The new script and VS Code tasks are fully self-contained and don't invoke `nmake deploy`, so they work regardless of the `DEPLOY` flag's state.
- **Ran successfully end-to-end (2026-07-31)**: full deploy — build, sign exe, `windeployqt`, package content, build installer, sign installer — completed via the "Deploy 12 Step Windows Installer" VS Code task. `CHANGELOG.md` was restored (`git checkout -- CHANGELOG.md`). Both the packaged exe and the installer verified with `Get-AuthenticodeSignature` → `Valid`.
- **Signing troubleshooting saga**: `signtool` failed repeatedly with `SignerSign() failed (-2147023170/0x800706be)`. Root cause turned out to be two independent things layered on top of each other: (1) `/fd certHash` in both `make-installer.ps1` and `12StepEditor.pro`'s `deploy`/`deploy_nosign` targets is not a valid digest algorithm (only `sha1`/`sha256` are) — copied from the `.pro` file's own never-actually-run deploy target; fixed to `/fd sha256`, dropped the redundant `/v /debug /sha1 <thumbprint>` to match the proven-working command in `SoftStep/shared/sendsysex/RELEASING.md`. (2) Separately, the actual code-signing cert (SafeNet/Certum USB token, thumbprint `66AEF8AC...`) had a stale local Windows-cert-store link to the CNG "Smart Card Key Storage Provider" (`certutil -user -store My <thumbprint>` showed "Certificate public key does NOT match private key"), even though the card itself verified fine under the legacy CAPI provider. `certutil -repairstore` didn't fix it (`CRYPT_E_EXISTS`); the user resolved it by reinstalling the cert via Certum's own utility. Both fixes were needed together.

## Firmware 1.0.9 + port renaming (2026-07-31)

Pulled `mk_firmware_tester`/`sendsysex` forward (`ed455b1`/`f3bb9bc`) to pick up firmware 1.0.9, which the shared device database (`sendsysex/data/families/12step.json`) documents as: adding a boot-time tare capture for key/select sensors, raising on-device on/off threshold defaults to 20/15 with an **enforced off ≥ on/2 floor** (a different constraint than the editor's own off ≤ on−1 gap rule — not yet reconciled in the editor, flagged to the user, no decision made yet), and — the big one — **renaming the unit's USB MIDI ports** from unnamed `"12Step"`/`"12Step 2"` (or `"12Step Port 1/2"` on Mac) to named `"12Step Control Surface"`/`"12Step Expander"`.
- App version → 3.0.8, `CHANGELOG.md` entry added.
- `12Step_Firmware_v1.0.9-cs512.syx` wired in (resources, `.qrc`, `_fw_ver_12step` → `1,0,9` in `KMI_FwVersions.h`); the `.pro`'s firmware filename pattern is version-driven so no further code change was needed there.
- Added `TWELVESTEP1_IN_P1_NAMED`/`OUT_P1_NAMED` (`"12Step Control Surface"`, same string cross-platform) to `KMI_DevData.h`, OR'd into all five port-name comparisons in `mainwindow.cpp`'s `slotMIDIPortChange()` (connect, disconnect, feedback-loop guard, Windows post-update reconnect check) alongside the existing bare-name checks, so units on either old or new firmware are still detected. Confirmed `portNameFix()` in `KMI_ports.cpp` doesn't mangle the new name on WMS or WinMM.

## Critical bug found + fixed via WinMM hardware log (2026-07-31)

First real WinMM hardware test (`12-Step-Editor-20260730-142834-485.log`) showed the firmware update appearing to hang/fail, requiring two attempts. Full trace: all 235 chunks sent successfully both times; the device's port closes as expected while it reboots bootloader→application; the app immediately tries to send a "final verification" SysEx identity request on that now-closed port, which fails (`sendSysExImmediate`'s `!port_out_open` check) — and the code sets `firmwareUpdateState = FWUD_STATE_FAIL` **without** calling `resetPacketizedFirmwareUpdate()` or clearing `packetizedFirmwarePhase`, so `isPacketizedFirmwareTransferActive()` kept returning true and something kept re-invoking the same failing attempt with no real delay. Result: a busy loop (627 failed attempts logged) lasting **3+ minutes**, only interrupted when the user gave up and retried. On the second attempt the device actually did reconnect with `FirmwareMatch` (confirming the flash genuinely succeeded both times), but the app never recognized success, so closing the stuck dialog logged `success: false` anyway. WinMM exposed this reliably because its port close→reopen during this transition took 18+ seconds in the log; WMS may reconnect fast enough to rarely hit it, but it's the same code path so isn't guaranteed safe.

**Fix in `KMI_mdm.cpp`** (uncommitted, not pushed): in both `FW_PACKET_PHASE_FINAL_DELAY` and the retry branch of `FW_PACKET_PHASE_WAIT_FINAL_VERIFY`, check `port_out_open` before attempting `sendFirmwareIdentityRequest()`. If the port isn't open, don't attempt the send or burn a retry — just reschedule the pump at a new `PACKETIZED_FW_PORT_POLL_INTERVAL_MS` (1000ms) poll interval, bounded by the existing `FW_UPDATE_TIMEOUT_INTERVAL` (90s, via `firmwareUpdateStateTimer`, already running since the last chunk was sent) — matching the generous "waiting for device to reappear" pattern already used elsewhere in this same file for the pre-chunking bootloader-entry wait. Once the port reopens, normal ACK-timeout/retry logic (3 retries × 5s, for "port open but no valid reply" cases) resumes unaffected. Rebuilt clean. **Not yet re-tested against real hardware** — the original log predates the WMS-transport-mode fix above, so this exact repro hasn't been re-run since either fix landed.

## GitHub branch port completed (2026-07-31)

All work described above (WMS/WinMM migration, chunked firmware, threshold rules, signing fixes, busy-loop fix, VS Code tooling, CHANGELOG entry) has been manually re-applied file-by-file onto the local `main` branch (tracking `github/main`), since `main_win`/Bitbucket and `github/main` had zero shared history. One gap found and fixed during the port: the github-based branch's independent `KMI_KMDM` consolidation never had `inc/KMI_KMDM/diagnosticlogger.cpp/h` or `inc/KMI_KMDM/KMI_fwupdate.cpp/h` added to `12StepEditor.pro`, causing 15 `LNK2019` errors on first rebuild — fixed by adding the 4 missing SOURCES/HEADERS lines. Also discovered `win-build/config/config.xml`, `pre-install.qs`, `style.qss`, `meta/package.xml`, `meta/installscript.qs`, and `data/Content/CHANGELOG.md` were tracked as **empty placeholder blobs** on both `main` and `main_win` — real content only ever existed as long-standing uncommitted local changes, preserved in `stash@{0}` and restored onto `main`. `main` now rebuilds clean (`qmake` + `nmake`, verified). **`github/main` is authoritative; nothing has been committed or pushed** — all of the above remains uncommitted working-tree state on `main`, per standing instruction.

## Installer update-repository hosted via GitHub Pages (2026-07-31)

Investigating "does an existing install offer an upgrade path" (it didn't — no update repository was ever configured) led to building one, hosted on GitHub Pages rather than GitHub Releases: Pages serves the repo tree byte-for-byte (no filename mangling — Releases renames spaces in asset names on upload, which would break QtIFW's exact-match `DownloadableArchives` references), and it's free for this public repo.

- `win-build/config/config.xml`: added `<RemoteRepositories>` pointing at `https://muse-kinetics.github.io/12-step-editor-qt6/update-repository`.
- `.vscode/make-update-repo.ps1`: new script wrapping `repogen` against `win-build/packages`, output to `win-build/update-repository/` (gitignored, added `/win-build/update-repository/` to `.gitignore`).
- **Validated end-to-end locally**: built a real unsigned 3.0.8 offline installer with the config pointed at `http://127.0.0.1:8899/...` instead, installed it to a scratch dir, served `win-build/update-repository` via `py -3 -m http.server`, confirmed `maintenancetool check-updates` correctly reports no updates at matching versions, bumped to 3.0.9 and confirmed it's detected and `maintenancetool update` actually replaces the files (`components.xml` shows 3.0.9 afterward). Reverted the version bump and the localhost URL back to real values afterward.
- Staged a `gh-pages` branch **locally only**, in a separate git worktree at `C:\Users\eric\gh-pages-staging` (kept isolated from the main working copy to avoid disturbing its uncommitted state) — orphan branch, committed locally with `.nojekyll`, a short `README.md`, and the real 3.0.8 `update-repository/` content. **Not pushed.**
- **Awaiting user approval** to (1) push `gh-pages` to the `github` remote and (2) enable GitHub Pages in repo settings (source: `gh-pages` branch, root) — both are real public actions not yet authorized.

## Remaining (SOP §5 — hardware validation, not done)

- WMS detection on a WMS-equipped machine; forced-WinMM detection on the same machine.
- **Re-run a full WinMM firmware update** with the port-close busy-loop fix in place — this is the highest-value remaining test given the bug just found.
- Hot-plug detect/debounce on the WinMM path.
- Confirm a sub-1.0.0-firmware device is blocked by the new guard (no real device known to be on such old firmware — may not be testable).
- Confirm generic SysEx (preset send) still works and is blocked/queued correctly during an active firmware transfer.
- Confirm both old-port and new-named-port (1.0.9+) devices are detected correctly.
- Decide whether to reconcile the editor's on/off threshold gap rule (off ≤ on−1) with firmware 1.0.9's on-device floor (off ≥ on/2) — flagged, not decided.
- Push the `KMI_KMDM` submodule changes (`KMI_FwVersions.h` fw-version bumps, `KMI_DevData.h` named-port defines, `KMI_mdm.cpp` transport-mode + busy-loop fixes) — all currently local-only uncommitted changes in that submodule's working tree, not committed/pushed to the shared `kmi_midi_device_manager` repo. Flag before pushing since it's a shared multi-product repo.

## Immediate Next Step

Two independent threads are open, neither blocking the other:
1. Get user sign-off to push the staged `gh-pages` branch and enable GitHub Pages, making the update repository live.
2. Hardware validation per SOP §5, ideally on a WMS-equipped Windows machine with a real 12 Step device — prioritize re-testing the WinMM firmware-update path given the busy-loop fix above.
